<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/assets/styles.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
  <style>
  <style>
    /* Vos styles CSS existants */
    .reservation-details {
      margin-bottom: 20px; /* Espacement vertical entre les détails des réservations */
    }
    .action-buttons {
      margin-top: 10px; /* Espacement entre les boutons d'action */
    }
    .action-buttons .btn {
      /*margin-right: 5px;*/ /* Espacement entre les boutons d'action */
      font-size: 0.8rem; /* Taille de police plus petite pour les autres boutons */
    }
  </style>
</head>
<body>

<div class="container">
  <h6 class="title">Liste des réservations</h6>
  <%= form_tag destroy_multiple_reservations_path, method: :delete do %>
    <% @reservations.each do |reservation| %>
      <div class="reservation-details">
        <%= check_box_tag "reservation_ids[]", reservation.id, false, class: "checkbox" %>
        <h5>Réservation ID: <%= reservation.id %></h5>
        <p><strong>Nom de la Salle:</strong> <%= reservation.salle.present? ? reservation.salle.name : "Salle inconnue" %></p>
        <p><strong>Date et Heure de Début:</strong> <%= reservation.start_time %></p>
        <p><strong>Date et Heure de Fin:</strong> <%= reservation.end_time %></p>
        <p><strong>Nom du Réservataire:</strong> <%= reservation.reserver_name %></p>
        <p><strong>Téléphone:</strong> <%= reservation.reserver_phone %></p>
        <p><strong>Email:</strong> <%= reservation.reserver_email %></p>
        <div class="actions">
          <%= link_to 'Show', reservation, class: "btn btn-primary btn-sm" %>
          <%= link_to 'Edit', edit_reservation_path(reservation), class: "btn btn-secondary btn-sm" %>
        </div>
      </div>
    <% end %>
    <div class="action-buttons">
      <%= link_to 'Retour', root_path, class: "btn btn-secondary" %>
      <%= button_tag "Delete selected reservations", class: "btn btn-danger" %>
    </div>
  <% end %>
</div>

<script>
  document.getElementById('checkAll').addEventListener('change', function() {
    const checkboxes = document.querySelectorAll('.checkbox');
    checkboxes.forEach(checkbox => {
      checkbox.checked = this.checked;
    });
  });
</script>

</body>
</html>
